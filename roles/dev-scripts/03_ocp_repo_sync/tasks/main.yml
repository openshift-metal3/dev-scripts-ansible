- name: run 03_ocp_repo_sync.sh
#  hosts:  virthost
  shell: "{{ dev_scripts_dir }}/03_ocp_repo_sync.sh" 
  args:
    chdir: "{{ dev_scripts_dir }}"
    creates: "{{ dev_scripts_dir }}/logs/03_ocp_repo_sync*.log"
